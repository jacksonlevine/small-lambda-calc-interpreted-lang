print (concat "There are " (+ 8 2) " balloons")
s var 8
print (concat .var (concat " is" " the" " number " (+ 0 1) " number."))

myfunc count name {
    p (concat "There are " .count .name "s!")
    s name "cat"
    p (concat "Name is now " .name) 
    }

print (concat "Test" " this is")


myfunc 12 "dog"